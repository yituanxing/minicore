AetherCore v2 F7 Linux interrupt observer exact-head probe.

request_revision=1
reason=Validate that clean-boundary async interrupts were hidden only by the shared runner commit.valid nesting; CPU RTL and unchanged Linux remain untouched.
